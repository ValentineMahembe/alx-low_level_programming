#include <stdio.h>

/**
 * main - Entry point
 * Description: print numbers 1 to 10
 * Return: 0 (succes)
 */
int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
